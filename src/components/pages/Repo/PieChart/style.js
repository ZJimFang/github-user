const options = {
  responsive: true,
  maintainAspectRatio: false,
  plugins: {
    legend: {
      display: false,
    },
  },
};

const clearIcon = {
  width: "80px",
  height: "80px",
};
export { options, clearIcon };
